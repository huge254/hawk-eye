# Grafana 数据源自动配置：启动即连通 Prometheus，无需手动添加
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus        # 固定 uid，供自带仪表盘引用
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
